from setuptools import setup

setup(
    name='head_first_python',
    version='1.0.0',
    author='Davirain',
    packages=[],
    install_requires=[
        # 'dependency1',
        # 'dependency2',
    ],
)
